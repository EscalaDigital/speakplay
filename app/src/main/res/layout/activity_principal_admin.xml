<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".principalAdminActivity">

    <Button
        android:id="@+id/estadisticasBoton"
        android:layout_width="197dp"
        android:layout_height="86dp"
        android:layout_marginTop="88dp"
        android:text="Consultar estadísticas"
        android:textColor="#FFFFFF"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <Button
        android:id="@+id/perfilesBoton"
        android:layout_width="197dp"
        android:layout_height="86dp"
        android:layout_marginTop="104dp"
        android:text="Consultar perfiles"
        android:textColor="#FFFFFF"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/estadisticasBoton" />

    <Button
        android:id="@+id/mapaBoton"
        android:layout_width="197dp"
        android:layout_height="86dp"
        android:layout_marginTop="104dp"
        android:text="Consultar mapa"
        android:textColor="#FFFFFF"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/perfilesBoton" />

</androidx.constraintlayout.widget.ConstraintLayout>